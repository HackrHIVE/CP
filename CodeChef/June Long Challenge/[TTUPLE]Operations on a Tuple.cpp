#include <bits/stdc++.h>
using namespace std;
int main()
{
    std::ios::sync_with_stdio(false);
    std::cout.tie(nullptr);
    std::cin.tie(nullptr);
    int t;
    long int a, b, c;
    long int p, q, r;
    cin >> t;
    while (t--)
    {
        cin >> p >> q >> r;
        cin >> a >> b >> c;
        
    }
    return 0;
}