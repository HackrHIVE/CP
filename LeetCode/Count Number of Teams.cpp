#include<bits/stdc++.h>
using namespace std;
int numTeams(vector<int>& rating) {
    //  i   <  j   <  k
    //arr[i]<arr[j]<arr[k]
    for(int )
}
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    vector <int> x({1,2,2,3,3,3});
    cout<<findLucky(x);
    return 0;
}
