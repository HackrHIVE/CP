#include <bits/stdc++.h>
using namespace std;
int minMoves(vector<vector<int>> maze, int x, int y)
{
    int totalGold = 0;

    for (int i = 0; i < maze.size(); i++)
        for (int j = 0; j < maze[0].size(); i++)
            if (maze[i][j] == 2)
                totalGold++;

        
}
int main()
{

    return 0;
}